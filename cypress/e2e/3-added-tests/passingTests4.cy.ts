describe("all succeed 1", { tags: "added" }, () => {
  it("succeed 1", () => {
    expect(true).to.equal(true);
  });

  it("succeed 2", () => {
    expect(true).to.equal(true);
  });

  it("succeed 3", () => {
    expect(true).to.equal(true);
  });

  it("succeed 4", () => {
    expect(true).to.equal(true);
  });

  it("succeed 5", () => {
    expect(true).to.equal(true);
  });
});
